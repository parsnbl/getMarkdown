import { test, expect, describe, vi } from "vitest";
import {
  getSelectedHtml,
  parseToMd,
  copyToTheClipboard,
  handleMessageBackground,
} from "../src/scripts/content.js";

test("getSelectedHtml", () => {});
